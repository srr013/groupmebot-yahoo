Relies on the heroku CLI

Deploy an app through: heroku create > git push heroku master?


To view logs:
Heroku logs --tail

dynos - heroku processes running
heroku ps:scale web= #

heoku local - starts processes locally
    waitress or another web server is needed to run this locally

Pushing code:
once the code is pushed to the git repo push it to Heroku vis "git push heroku master"
(requires you to init heroku CLI with git repo: heroku git:remote -a your-app-name )
start the logs and keep testing

To start:
heroku local web -f Procfile.windows